# Веб-сервис автоматизации клиентских обращений ООО «ГК «ИРБИС»»

Проект представляет собой пользовательское Flask-приложение для автоматизации взаимодействия с клиентами строительной компании. Сервис позволяет принимать заявки, сопровождать обращения, хранить комментарии, собирать обратную связь и автоматически определять тональность клиентского сообщения с помощью заранее обученной модели.

Папка `models` в архиве содержит артефакты, полученные в Google Colab:

```text
models/model.joblib
models/model_info.json
models/aspect_keywords.json
```

Если файлы модели еще не добавлены, приложение запустится в демонстрационном режиме и покажет предупреждение в административной панели. После добавления артефактов перезапустите сервер.

## Основные возможности

- публичные страницы компании, услуг, проектов, контактов и справочной информации;
- регистрация и авторизация пользователей;
- три роли доступа: клиент, менеджер, администратор;
- личный кабинет клиента с заявками, комментариями и отзывами;
- прикрепление и защищенное скачивание файлов по заявкам;
- панель менеджера для обработки заявок, смены статусов и анализа обратной связи;
- сохранение сообщений из публичной формы и управление их статусами;
- административная панель с контролем пользователей, услуг и состояния модели;
- управление проектами и системными настройками;
- SQLite-база данных для хранения пользователей, заявок, комментариев, отзывов и журнала действий;
- персональные уведомления об изменениях в заявках;
- автоматическое определение тональности отзывов через `model.joblib`;
- определение тематического аспекта обращения через `aspect_keywords.json`;
- отчеты по статусам заявок, услугам, тональности и аспектам обращений;
- формирование отчетов по заявкам, отзывам и услугам в форматах DOCX и XLSX;
- формирование подробного CSV-реестра заявок для скачивания.

## Структура проекта

```text
irbis_client_service/
├── main.py
├── config.py
├── requirements.txt
├── README.md
├── app/
│   ├── __init__.py
│   ├── extensions.py
│   ├── domain/
│   ├── routes/
│   ├── services/
│   ├── database/
│   ├── forms/
│   └── utils/
├── templates/
├── static/
├── models/
├── instance/
├── uploads/
├── reports/
└── tests/
```

## Установка

Рекомендуемая версия Python: 3.9.12 или выше в пределах той же ветки зависимостей, в которой была сохранена модель.

```bash
python -m venv .venv
```

Для Windows:

```bash
.venv\Scripts\activate
```

Для Linux и macOS:

```bash
source .venv/bin/activate
```

Установка зависимостей:

```bash
pip install -r requirements.txt
```

## Подключение модели

Добавьте в папку `models` три файла из архива, выгруженного из Google Colab:

```text
models/model.joblib
models/model_info.json
models/aspect_keywords.json
```

Файл `model.joblib` содержит обученную модель анализа тональности. Файл `model_info.json` хранит сведения о классах, метриках и параметрах. Файл `aspect_keywords.json` содержит словарь тематических аспектов.

## Запуск

```bash
python main.py
```

После запуска откройте адрес:

```text
http://127.0.0.1:5000
```

При первом запуске приложение создаст SQLite-базу данных и заполнит ее демонстрационными пользователями, услугами, заявками и отзывами.

## Проверка

Для запуска автоматических тестов установите зависимости разработки и выполните:

```bash
pip install -r requirements-dev.txt
pytest -q
```

Тесты проверяют разграничение ролей, публичные маршруты, контактную форму, структуру базы данных, анализ отзывов, загрузку и скачивание файлов, а также формирование отчетов CSV, DOCX и XLSX.

## Демонстрационные учетные записи

| Роль | Логин | Пароль |
|---|---|---|
| Администратор | admin | admin1234 |
| Менеджер | manager | manager1234 |
| Клиент | client | client1234 |

## Основные маршруты

| Раздел | Адрес |
|---|---|
| Главная страница | `/` |
| Услуги | `/services` |
| Проекты | `/projects` |
| Как мы работаем | `/workflow` |
| Отзывы клиентов | `/reviews` |
| Контакты | `/contacts` |
| Регистрация | `/auth/register` |
| Вход | `/auth/login` |
| Кабинет клиента | `/client/dashboard` |
| Профиль клиента | `/client/profile` |
| Кабинет менеджера | `/manager/dashboard` |
| Клиенты менеджера | `/manager/clients` |
| Сообщения обратной связи | `/manager/contact-messages` |
| Страница отчетов | `/manager/reports` |
| Формирование DOCX/XLSX | `/manager/reports/generate` |
| CSV-отчет по заявкам | `/manager/reports/export.csv` |
| Административная панель | `/admin/dashboard` |
| Управление проектами | `/admin/projects` |
| Системные настройки | `/admin/settings` |
| Журнал действий администратора | `/admin/audit-log` |
| Уведомления пользователя | `/notifications/` |

## База данных

При запуске `db.create_all()` создает недостающие таблицы, не удаляя существующие данные. В рабочей схеме используются таблицы:

```text
users
services
projects
client_requests
request_comments
feedback
attachments
notifications
contact_messages
system_settings
audit_log
```

Если база создается впервые, `seed_database()` добавляет демонстрационных пользователей, услуги, проекты, заявку, отзыв, контактное сообщение, системные настройки, уведомление и пример файла заявки.

## Работа с файлами

К заявке можно приложить PDF, PNG, JPG, DOC, DOCX, XLS, XLSX или TXT размером до 16 МБ. Файлы получают уникальные имена, сохраняются внутри `uploads/requests/<номер заявки>` и скачиваются только владельцем заявки, менеджером или администратором. Метаданные файлов хранятся в таблице `attachments`.

Локальное файловое хранилище подходит для учебного запуска и сервера с постоянным диском. При размещении на платформе с временной файловой системой необходимо подключить постоянный диск либо объектное хранилище.

## Формирование отчетов

В кабинете менеджера доступны три типа отчетов: сводка по заявкам, анализ отзывов и отчет по услугам. Каждый отчет можно сформировать в формате DOCX или XLSX. Файл получает уникальное имя с датой и временем, сохраняется в каталоге `reports/` и сразу передается пользователю для скачивания. Ранее сформированные файлы отображаются на странице отчетов. CSV-реестр заявок сохранен как отдельная компактная выгрузка.

## Работа интеллектуального модуля

При создании отзыва приложение выполняет три действия. Текст очищается от лишних пробелов и служебных символов, затем модель определяет тональность, после чего словарь аспектов выделяет тему обращения. Результат сохраняется в таблице отзывов и отображается менеджеру.

Если файл `model.joblib` отсутствует, применяется упрощенное правило на основе словарей. Такой режим нужен только для демонстрации интерфейса до подключения обученной модели.

## Назначение для ВКР

Проект можно использовать как практическую часть ВКР по теме «Совершенствование процесса взаимодействия с клиентами компании с помощью веб-технологий». Веб-сервис демонстрирует процесс обработки обращения: клиентская заявка, назначение услуги, комментарии, смена статуса, отзыв, анализ тональности и управленческая отчетность.
